# ============================================================
# Time Trackinator — Environment Variables
# Copy this file to .env and fill in your values.
# ============================================================

# ------------------------------------------------------------------
# REQUIRED: Flask secret key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# ------------------------------------------------------------------
SECRET_KEY=change-me-to-a-long-random-string

# ------------------------------------------------------------------
# OAuth — Microsoft (Azure AD)
# Register an app at https://portal.azure.com → Azure Active Directory → App Registrations
# Redirect URI to add: https://yourdomain.com/callback/microsoft
# ------------------------------------------------------------------
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
# Use "common" for any Microsoft account, or your tenant ID to restrict to your org
MICROSOFT_TENANT_ID=common

# ------------------------------------------------------------------
# OAuth — Google
# Register an app at https://console.cloud.google.com → APIs & Services → Credentials
# Redirect URI to add: https://yourdomain.com/callback/google
# ------------------------------------------------------------------
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ------------------------------------------------------------------
# Access Control
# ------------------------------------------------------------------
# Comma-separated list of email addresses that get admin access on first login
ADMIN_EMAILS=boss@example.com,supervisor@example.com

# (Optional) Restrict sign-in to specific email domains. Leave blank to allow any.
# Example: ALLOWED_DOMAINS=example.com,contractor.com
ALLOWED_DOMAINS=

# ------------------------------------------------------------------
# Server
# ------------------------------------------------------------------
# Host port to expose (default 5000)
PORT=5000

# Timezone for the server — MUST match your department's local time zone
# so that clock-in/out times display correctly.
# Examples: America/New_York, America/Chicago, America/Denver, America/Los_Angeles
TZ=America/New_York
